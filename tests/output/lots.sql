CREATE TABLE LOTS_3 (
	PropertyID INT,
	CountyName VARCHAR(255),
	LotNumber INT,
	Area VARCHAR(255),
	PRIMARY KEY (PropertyID)
);

CREATE TABLE LOTS_4 (
	Area VARCHAR(255),
	Price REAL,
	PRIMARY KEY (Area)
);

CREATE TABLE LOTS_2 (
	CountyName VARCHAR(255),
	TaxRate REAL,
	PRIMARY KEY (CountyName)
);